# Структура ML микросервиса

Полное описание структуры папки `ml/` для понимания другими LLM и разработчиками.

## 📁 Структура директорий

```
ml/
├── app/                          # Основное приложение
│   ├── main.py                   # Точка входа FastAPI
│   ├── core/
│   │   └── config.py            # Конфигурация (API ключи, модели)
│   ├── models/
│   │   └── schemas.py           # Pydantic схемы для валидации
│   ├── routes/
│   │   └── api.py               # API эндпоинты
│   └── services/
│       ├── llm_client.py        # Клиент для LLM API
│       ├── task_generator.py    # Генерация задач
│       ├── hint_service.py      # Система подсказок (NEW!)
│       ├── evaluator.py         # Оценка кода
│       ├── communication.py     # Оценка коммуникации
│       ├── scoring.py           # Расчет финального балла
│       ├── adaptive_engine.py   # Адаптивная сложность
│       └── anti_cheat.py        # Проверка на плагиат
├── .env                         # Переменные окружения
├── requirements.txt             # Python зависимости
├── README.md                    # Общий обзор
├── INTEGRATION.md               # Интеграция с бэкендом
├── API_GUIDE.md                 # Документация API
├── HINTS_GUIDE.md               # Руководство по подсказкам
├── QUICKSTART.md                # Быстрый старт
├── CHANGELOG.md                 # История изменений
├── TROUBLESHOOTING.md           # Решение проблем
└── test_*.sh                    # Тестовые скрипты
```

---

## 📄 Описание файлов

### Основные файлы приложения

#### `app/main.py`
**Назначение:** Точка входа FastAPI приложения

**Что делает:**
- Инициализирует FastAPI приложение
- Настраивает CORS
- Подключает роутеры из `api.py`
- Определяет health check эндпоинт

**Ключевые элементы:**
```python
app = FastAPI(title="VibeCode Jam ML Service")
app.include_router(router, prefix="/api/v1")

@app.get("/health")
async def health_check():
    return {"status": "healthy"}
```

---

#### `app/core/config.py`
**Назначение:** Конфигурация приложения

**Что содержит:**
- API ключи для SciBox
- Базовые URL для LLM API
- Названия моделей
- Настройки из `.env`

**Важные переменные:**
```python
class Settings(BaseSettings):
    SCIBOX_API_KEY: str
    SCIBOX_API_BASE: str = "https://api.scibox.com/v1"
    MODEL_AWQ: str = "qwen3-32b-awq"          # Для генерации задач
    MODEL_CODER: str = "qwen3-coder-30b-..."  # Для оценки кода
```

---

#### `app/models/schemas.py`
**Назначение:** Pydantic схемы для валидации данных

**Что содержит:**
- `Task` - схема задачи (с hints!)
- `Hint` - схема подсказки (level, content, penalty)
- `EvaluationRequest/Response` - для оценки кода
- `ScoringRequest/Response` - для расчета баллов (с hints_used!)
- `CommunicationRequest/Response` - для оценки коммуникации
- `GenerateHintsRequest/Response` - для генерации подсказок

**Ключевые схемы:**
```python
class Task(BaseModel):
    title: str
    description: str
    difficulty: Literal["easy", "medium", "hard"]
    hints: Optional[List[Dict[str, Any]]] = None  # 3 уровня

class Hint(BaseModel):
    level: Literal["surface", "medium", "deep"]
    content: str
    penalty: float  # 5.0, 15.0, 30.0

class ScoringRequest(BaseModel):
    difficulty: str
    tests_passed: int
    total_tests: int
    time_taken_seconds: float
    code_quality_score: float
    communication_score: float
    hints_used: Optional[List[str]] = []  # ВАЖНО!
```

---

#### `app/routes/api.py`
**Назначение:** Определение всех API эндпоинтов

**Эндпоинты:**
1. `POST /generate-task` - генерация задачи с подсказками
2. `POST /evaluate` - оценка кода
3. `POST /communication/evaluate` - оценка коммуникации
4. `POST /communication/follow-up` - генерация follow-up вопроса
5. `POST /score` - расчет финального балла (с учетом подсказок!)
6. `POST /anti-cheat/check` - проверка на плагиат
7. `POST /adaptive/next-level` - адаптивная сложность
8. `POST /hints/generate` - генерация подсказок отдельно
9. `POST /generate-task-mock` - mock для тестирования

**Пример:**
```python
@router.post("/generate-task", response_model=Task)
async def generate_task(request: TaskGenerationRequest):
    task = await task_generator.generate_task(request.difficulty)
    return task  # Включает hints!

@router.post("/score", response_model=ScoringResponse)
async def calculate_score(request: ScoringRequest):
    final_score = scoring_service.calculate_final_score(
        difficulty=request.difficulty,
        # ...
        hints_used=request.hints_used  # Передаем подсказки!
    )
    return ScoringResponse(final_score=final_score)
```

---

### Сервисы (app/services/)

#### `llm_client.py`
**Назначение:** Клиент для взаимодействия с LLM API (SciBox)

**Основные методы:**
- `generate(messages, model, temperature)` - генерация текста
- `generate_json(messages, model, temperature)` - генерация JSON

**Особенности:**
- Обработка ошибок подключения
- Парсинг JSON из ответов LLM
- Очистка от тегов `<think>` и markdown блоков
- Обработка вложенного JSON в поле `content`

**Использование:**
```python
result = await llm_client.generate_json(
    model=settings.MODEL_AWQ,
    messages=[
        {"role": "system", "content": "Ты эксперт..."},
        {"role": "user", "content": prompt}
    ],
    temperature=0.7
)
```

---

#### `task_generator.py`
**Назначение:** Генерация алгоритмических задач

**Основные методы:**
- `generate_task(difficulty)` - генерирует задачу с подсказками
- `_generate_hidden_tests(task_data)` - генерирует скрытые тесты
- `_get_generation_prompt(difficulty)` - формирует промпт

**Процесс генерации:**
1. Генерирует описание задачи (MODEL_AWQ)
2. Генерирует скрытые тесты (MODEL_CODER)
3. **Генерирует 3 уровня подсказок** (hint_service)
4. Возвращает полную задачу

**Важно:** Все промпты на русском языке!

---

#### `hint_service.py` 🆕
**Назначение:** Генерация системы подсказок трех уровней

**Основной метод:**
- `generate_hints(task_description, difficulty, input_format, output_format, examples)`

**Что делает:**
1. Формирует промпт с инструкциями для LLM
2. Просит сгенерировать 3 уровня подсказок:
   - **surface** - общее направление (-5 баллов)
   - **medium** - подход и структура (-15 баллов)
   - **deep** - подробный алгоритм (-30 баллов)
3. Возвращает список из 3 объектов Hint

**Штрафы:**
```python
PENALTY_SURFACE = 5.0
PENALTY_MEDIUM = 15.0
PENALTY_DEEP = 30.0
```

**Промпт на русском:**
```python
prompt = f"""
Сгенерируй ТРИ подсказки разного уровня...

1. ПОВЕРХНОСТНАЯ (surface) - Общее направление...
2. СРЕДНЯЯ (medium) - Более конкретная...
3. ГЛУБОКАЯ (deep) - Почти полное решение...

ВАЖНО: Все подсказки на РУССКОМ ЯЗЫКЕ
"""
```

---

#### `evaluator.py`
**Назначение:** Оценка кода кандидата

**Основной метод:**
- `evaluate_solution(request)` - оценивает код

**Что оценивает:**
- Корректность (correctness_score)
- Эффективность (efficiency_score)
- Качество кода (clean_code_score)
- Обратная связь на русском (feedback)

**Промпт на русском:**
```python
prompt = f"""
Оцени следующее решение алгоритмической задачи.

Описание задачи: {request.task_description}
Код пользователя: {request.code}

Проанализируй корректность, эффективность и качество кода.
ВАЖНО: Обратная связь на РУССКОМ ЯЗЫКЕ.
"""
```

---

#### `communication.py`
**Назначение:** Оценка коммуникативных навыков

**Основные методы:**
- `evaluate_explanation(problem_desc, user_explanation, code)` - оценка объяснения
- `generate_followup_question(problem_desc, code)` - генерация follow-up вопроса

**Что оценивает:**
- Ясность объяснения
- Корректность терминологии
- Логика изложения
- Соответствие объяснения коду

**Возвращает:**
- `communication_score` (0.0-1.0)
- `feedback` на русском

---

#### `scoring.py`
**Назначение:** Расчет финального балла

**Основной метод:**
- `calculate_final_score(difficulty, tests_passed, total_tests, time_taken_seconds, code_quality_score, communication_score, hints_used)`

**Формула:**
```python
# Нормализация метрик к 0-1
correctness = tests_passed / total_tests
code_quality = code_quality_score / 100.0
communication = communication_score / 100.0

# Временной балл
time_limits = {"easy": 300, "medium": 600, "hard": 900}
time_score = max(0.0, 1.0 - (time_taken_seconds / time_limit) * 0.5)

# Базовый балл
score = (
    0.40 * correctness +
    0.20 * code_quality +
    0.20 * communication +
    0.20 * time_score
)

# Множитель сложности
multiplier = {"easy": 1.0, "medium": 1.2, "hard": 1.5}
final_score = score * multiplier * 100.0

# ВЫЧИТАЕМ ШТРАФЫ ЗА ПОДСКАЗКИ!
hint_penalties = {"surface": 5.0, "medium": 15.0, "deep": 30.0}
total_penalty = sum(hint_penalties[hint] for hint in hints_used)
final_score -= total_penalty

return max(0.0, min(100.0, final_score))
```

**Важно:** Штрафы за подсказки вычитаются из финального балла!

---

#### `adaptive_engine.py`
**Назначение:** Определение следующего уровня сложности

**Основной метод:**
- `determine_next_level(current_difficulty, passed_tests, total_tests, bad_attempts, total_time_seconds)`

**Логика:**
- Анализирует производительность кандидата
- Рекомендует повышение/понижение сложности
- Возвращает `next_difficulty` и `reason` на русском

---

#### `anti_cheat.py`
**Назначение:** Проверка кода на плагиат и AI-генерацию

**Основной метод:**
- `check_submission(code, problem_desc)`

**Что проверяет:**
- Комментарии из другого контекста
- Слишком общие имена переменных
- Логика, решающая другую задачу

**Возвращает:**
- `is_suspicious` (bool)
- `confidence` (0.0-1.0)
- `reason` на русском

**Промпт на русском:**
```python
prompt = f"""
Проанализируй следующий код на признаки нечестного решения...
ВАЖНО: Причина на РУССКОМ ЯЗЫКЕ.
"""
```

---

## 🔧 Конфигурационные файлы

### `.env`
**Назначение:** Переменные окружения

**Содержимое:**
```bash
SCIBOX_API_KEY=your_api_key_here
SCIBOX_API_BASE=https://api.scibox.com/v1
MODEL_AWQ=qwen3-32b-awq
MODEL_CODER=qwen3-coder-30b-a3b-instruct-fp8
```

**Важно:** Этот файл переопределяет настройки из `config.py`

---

### `requirements.txt`
**Назначение:** Python зависимости

**Основные пакеты:**
```
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
httpx==0.25.1
python-dotenv==1.0.0
```

---

## 📚 Документация

### `README.md`
**Содержит:**
- Общий обзор проекта
- Список функций (включая систему подсказок)
- Инструкции по установке
- Примеры использования
- Ссылки на другую документацию

---

### `INTEGRATION.md`
**Содержит:**
- Как интегрировать ML сервис с бэкендом
- Примеры кода для Node.js/Express
- Модели данных для MongoDB
- Типичный flow интервью
- Обработка подсказок в бэкенде

---

### `API_GUIDE.md`
**Содержит:**
- Подробное описание всех эндпоинтов
- Примеры запросов и ответов
- Коды ошибок
- Примеры использования через curl

---

### `HINTS_GUIDE.md`
**Содержит:**
- Описание системы подсказок
- Три уровня подсказок
- Штрафы за использование
- Влияние на рейтинг
- Примеры подсказок
- Рекомендации по использованию

---

### `QUICKSTART.md`
**Содержит:**
- Быстрый старт проекта
- Установка зависимостей
- Запуск сервера
- Первые тесты
- Проверка работоспособности

---

### `CHANGELOG.md`
**Содержит:**
- История изменений
- Новые функции (система подсказок!)
- Исправления багов
- Обновления API

---

### `TROUBLESHOOTING.md`
**Содержит:**
- Решение типичных проблем
- Ошибки подключения к LLM API
- Проблемы с парсингом JSON
- Отладка

---

## 🧪 Тестовые скрипты

### `test_russian.sh`
**Тестирует:** Генерацию задач на русском языке

### `test_evaluation_russian.sh`
**Тестирует:** Оценку решений и коммуникации на русском

### `test_scoring.sh`
**Тестирует:** Расчет баллов с разными параметрами

### `test_hints.sh` 🆕
**Тестирует:** 
- Генерацию задачи с подсказками
- Расчет баллов БЕЗ подсказок
- Расчет баллов С ОДНОЙ подсказкой
- Расчет баллов СО ВСЕМИ подсказками

**Пример использования:**
```bash
chmod +x test_hints.sh
./test_hints.sh
```

### `test_anticheat_russian.sh`
**Тестирует:** Анти-чит проверку на русском языке

---

## 🔑 Ключевые концепции

### 1. Система подсказок (Hints System)

**Как работает:**
1. При генерации задачи автоматически создаются 3 подсказки
2. Подсказки хранятся в задаче, но НЕ отправляются фронтенду
3. Кандидат может запросить подсказку через бэкенд
4. Каждая подсказка используется только один раз
5. При расчете финального балла штрафы вычитаются

**Уровни:**
- **surface** - Поверхностная (общая идея) - штраф 5 баллов
- **medium** - Средняя (подход и структура) - штраф 15 баллов
- **deep** - Глубокая (подробный алгоритм) - штраф 30 баллов

**Пример подсказок для задачи "Найти максимум":**
```python
hints = [
    {
        "level": "surface",
        "content": "Подумай о переборе всех элементов и отслеживании максимума",
        "penalty": 5.0
    },
    {
        "level": "medium",
        "content": "Используй переменную для хранения текущего максимума и обновляй её в цикле",
        "penalty": 15.0
    },
    {
        "level": "deep",
        "content": "1) Инициализируй max_val первым элементом. 2) Пройди по остальным. 3) Если элемент > max_val, обнови max_val. 4) Верни max_val",
        "penalty": 30.0
    }
]
```

---

### 2. Русский язык

**Все тексты на русском:**
- Описания задач
- Подсказки
- Обратная связь (feedback)
- Причины анти-чит проверки
- Follow-up вопросы

**Достигается через:**
- Промпты на русском в системных сообщениях
- Явные инструкции "Всегда отвечай на русском языке"
- Инструкции "ВАЖНО: Верни текст НА РУССКОМ ЯЗЫКЕ"

---

### 3. Формула расчета баллов

**Компоненты:**
```
Базовый балл = (
  40% × корректность (tests_passed / total_tests) +
  20% × качество кода (code_quality_score / 100) +
  20% × коммуникация (communication_score / 100) +
  20% × время (зависит от лимита)
) × множитель_сложности × 100

Множители:
- easy: ×1.0
- medium: ×1.2
- hard: ×1.5

Штрафы за подсказки:
- surface: -5
- medium: -15
- deep: -30

Финальный балл = Базовый балл - Сумма штрафов
```

**Пример:**
```
Базовый: 92.4
Использованы: ["surface", "medium"]
Штраф: -5 - 15 = -20
Финальный: 92.4 - 20 = 72.4
```

---

## 🚀 Запуск проекта

### 1. Установка
```bash
cd ml
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### 2. Настройка
Создайте `.env` файл:
```bash
SCIBOX_API_KEY=your_key_here
```

### 3. Запуск
```bash
uvicorn app.main:app --reload --port 8001
```

### 4. Проверка
```bash
curl http://localhost:8001/health
open http://localhost:8001/docs
```

---

## 🔗 Связь между компонентами

```
main.py
  ↓
api.py (роутер)
  ↓
├─ task_generator.py → hint_service.py → llm_client.py
├─ evaluator.py → llm_client.py
├─ communication.py → llm_client.py
├─ scoring.py (использует hints_used!)
├─ adaptive_engine.py
└─ anti_cheat.py → llm_client.py

Все сервисы используют:
- config.py (настройки)
- schemas.py (валидация)
- llm_client.py (общение с LLM)
```

---

## ⚠️ Важные замечания

1. **Подсказки генерируются автоматически** при создании задачи
2. **Не отправляйте hints фронтенду** - храните в БД
3. **Передавайте hints_used** при расчете финального балла
4. **Все промпты на русском** - не меняйте язык
5. **Штрафы вычитаются автоматически** в scoring_service
6. **API ключ обязателен** - без него сервис не работает
7. **Используйте Swagger UI** для тестирования: http://localhost:8001/docs

---

## 📞 Контакты и поддержка

- **Swagger UI:** http://localhost:8001/docs
- **Health Check:** http://localhost:8001/health
- **Документация:** См. файлы в корне проекта
- **Тесты:** Запустите `./test_hints.sh`
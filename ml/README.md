# ML Микросервис для VibeCode Jam

Этот сервис отвечает за все AI-функции платформы, включая:
- 🎯 Генерацию алгоритмических задач **на русском языке** (Легкий/Средний/Сложный уровни)
- 💡 **Систему подсказок трех уровней** с автоматическим штрафом за использование
- 🔄 Адаптивную логику сложности
- ✅ Оценку и проверку кода
- 💬 Оценку коммуникативных навыков
- 🧪 Генерацию скрытых тестов
- 🛡️ Анти-чит проверку

**Особенность:** Все задачи генерируются на русском языке с использованием LLM моделей SciBox.

## Установка и запуск

1. **Установка зависимостей:**
   ```bash
   pip install -e .
   ```
   или
   ```bash
   pip install -r requirements.txt
   ```

2. **Переменные окружения:**
   Создайте файл `.env` с настройками SciBox API:
   ```bash
   SCIBOX_API_KEY=sk-c7K8ClMXslvPl6SRw2P9Ig
   SCIBOX_API_BASE=https://llm.t1v.scibox.tech/v1
   ```

3. **Запуск сервера:**
   ```bash
   cd ml
   source venv/bin/activate  # если используете venv
   uvicorn app.main:app --reload --port 8001
   ```

4. **Проверка работоспособности:**
   ```bash
   # Health check
   curl http://localhost:8001/health
   
   # Тестирование генерации на русском
   ./test_russian.sh
   ```

## API Эндпоинты

- `POST /api/v1/generate-task`: Генерация новой задачи **на русском языке** на основе уровня сложности
- `POST /api/v1/generate-task-mock`: Mock эндпоинт для тестирования без LLM
- `POST /api/v1/evaluate`: Оценка решения кода с помощью скрытых тестов и AI-анализа
- `POST /api/v1/adaptive-engine`: Определение следующего уровня сложности на основе результатов
- `POST /api/v1/communication/evaluate`: Оценка объяснения решения пользователем
- `POST /api/v1/communication/follow-up`: Генерация дополнительного вопроса для интервью
- `POST /api/v1/score`: Расчет итогового взвешенного балла за сессию интервью
- `POST /api/v1/anti-cheat/check`: Проверка кода на плагиат или AI-генерацию
- `GET /health`: Проверка работоспособности сервиса

**Подробная документация:** См. [API_GUIDE.md](API_GUIDE.md)

**Swagger UI:** `http://localhost:8001/docs`

## Архитектура и принцип работы

### 1. Генерация задач (TaskGenerator)
Использует модель **qwen3-32b-awq** для создания описаний задач и **qwen3-coder-30b** для генерации тестовых случаев.

**Уровни сложности (все на русском языке):**
- **Easy (Легкий)**: Простые операции с массивами, строками, базовая математика (O(n) или O(1))
  - Примеры: "Найти максимальный элемент", "Подсчёт чётных чисел", "Перевернуть строку"
- **Medium (Средний)**: Хеш-таблицы, два указателя, скользящее окно, вложенные циклы (O(n²) или O(n log n))
  - Примеры: "Наибольший общий префикс", "Первый уникальный символ", "Переместить нули"
- **Hard (Сложный)**: Реализация алгоритмов сортировки, рекурсия, деревья, основы динамического программирования
  - Примеры: "Реализация QuickSort", "Сортировка слиянием для деревьев", "Поиск в глубину"

### 2. Адаптивный движок (AdaptiveEngine)
Детерминированная логика для переходов между уровнями:
- **Старт**: Всегда начинается с уровня Medium
- **Если прошел Medium**: переход на Hard (если мало ошибок) или остается на Medium (если 2+ плохих попытки)
- **Если провалил Medium**: переход на Easy
- **Если прошел Easy**: возврат на Medium
- **Если провалил Easy**: остается на Easy
- **Если провалил Hard**: возврат на Medium
- **Если прошел Hard**: остается на Hard

### 3. Оценщик кода (Evaluator)
Запускает код в песочнице (базовое выполнение Python) и использует **qwen3-coder-30b** для анализа:
- **Корректность**: Проверка выходных данных на соответствие логике задачи
- **Эффективность**: Оценка временной сложности алгоритма
- **Качество кода**: Читаемость, использование pythonic-подходов

### 4. Коммуникация (Communication)
Использует **qwen3-32b-awq** для имитации диалога с интервьюером:
- Генерация follow-up вопросов о сложности, граничных случаях, оптимизациях
- Оценка объяснений кандидата (ясность, корректность терминологии, логика)

### 5. Система оценки (Scoring)
Агрегирует метрики для формирования финального балла:
```
final_score = 0.45 × correctness + 
              0.15 × efficiency + 
              0.10 × reliability + 
              0.10 × communication + 
              0.10 × time_score - 
              0.10 × bad_attempt_penalty
```

### 6. Анти-чит (Anti-Cheat)
Использует AI для обнаружения:
- Плагиата из стандартных туториалов
- Кода, сгенерированного AI без понимания
- Несоответствий между кодом и объяснением

## Модели LLM

Сервис использует две модели от SciBox:
- **qwen3-32b-awq**: Универсальная чат-модель (2 RPS) для Q&A, генерации текстов, диалогов
- **qwen3-coder-30b-a3b-instruct-fp8**: Специализированная модель для кода (2 RPS) для ревью, генерации и анализа кода

## Интеграция с Backend

ML-сервис работает как независимый микросервис и взаимодействует с основным backend через REST API.
Backend может вызывать эндпоинты ML-сервиса для:
1. Генерации задач для кандидатов
2. Проверки решений
3. Адаптации сложности в реальном времени
4. Получения финальных оценок

## Документация

- 📖 [QUICKSTART.md](QUICKSTART.md) - Быстрый старт и основные команды
- 📚 [API_GUIDE.md](API_GUIDE.md) - Подробное описание всех API эндпоинтов
- 💡 [HINTS_GUIDE.md](HINTS_GUIDE.md) - Руководство по системе подсказок
- 🔧 [TROUBLESHOOTING.md](TROUBLESHOOTING.md) - Решение проблем
- 📝 [CHANGELOG.md](CHANGELOG.md) - История изменений

## Примеры использования

### Генерация задачи на русском
```bash
curl -X POST "http://localhost:8001/api/v1/generate-task" \
  -H "Content-Type: application/json" \
  -d '{"difficulty": "easy"}' | jq .
```

### Тестирование всех уровней
```bash
./test_russian.sh
```

### Проверка работоспособности
```bash
curl http://localhost:8001/health
```

## Технологии

- **FastAPI** - веб-фреймворк
- **Pydantic** - валидация данных
- **httpx** - HTTP клиент для LLM API
- **SciBox LLM API** - модели qwen3-32b-awq и qwen3-coder-30b

## Лицензия

MIT
